# CLAUDE.md

このファイルは、このリポジトリで作業する際にClaude Codeが従うガイドラインです。

## リポジトリ

- リモート: https://github.com/koizoom1/samurai_taskboard.git

## Git運用ルール

**コードを変更したら、その都度GitHubにプッシュすること。**

- 1つの変更(タスク・修正単位)が完了するごとに、以下を実行する。
  1. `git status` / `git diff` で変更内容を確認する
  2. 関連するファイルのみをステージする(`git add -A` は避け、ファイルを明示的に指定する)
  3. 変更内容が伝わる簡潔なコミットメッセージを付けてコミットする
  4. `git push` でリモート(origin)にプッシュする
- 変更を溜め込んで大きなコミットにまとめない。作業が一段落するたびにコミット&プッシュする。
- コミット前に、意図しないファイル(secrets, .env, 一時ファイルなど)が含まれていないか確認する。
- force push (`--force`)、履歴の書き換え(`rebase -i`, `reset --hard` 等)は行わない。必要な場合は必ず事前にユーザーに確認する。
- pre-commit/pre-pushフックが失敗した場合は `--no-verify` で回避せず、原因を修正してから再度コミット・プッシュする。
- push前にリモートの状態を確認し、コンフリクトが発生する場合は安易に上書きせず、ユーザーに相談する。

## 開発時の注意

- 変更は小さく保ち、1コミット1目的を意識する。
- 既存のコードスタイル・命名規則に合わせる。
